*.dat
tools/
.DS_Store
.superpowers/
.idea/
bios/
.venv/
/capture-*.log
flycast-*.txt
__pycache__/
build/
shims/data/
loader/*.o
loader/*.elf

# ROM/commercial disc data — reference GDIs, dumps, mastered images (never commit)
*.zip
*.7z
*.gdi
*.cdi
track*.bin
track*.raw
# Ghidra Program XML companion image (byte-identical to boot.bin — copyrighted)
*.bytes

# SDD scratch (briefs, reports, review packages, progress ledger)
.superpowers/
loader/splash.png
# final review: donor extraction anywhere + BIOS-splash capture frames
*.iso
naomi_boot*.png
# disc cover art (third-party fan art, optional build input)
0GDTEX.png
*.pvr
